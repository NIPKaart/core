[tool.poetry]
name = "nipkaart-import-contract-proof"
version = "1.0.0"
description = "Offline cross-language contract proof, not a producer"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.14,<3.15"
jsonschema = "^4.26"
